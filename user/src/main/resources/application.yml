spring:
  application:
    name: user-service

  data:
    mongodb:
      uri: mongodb+srv://jladva0:GEYDs82Jz0RWbBcS@jaydeep-cluster-microse.df5pyrj.mongodb.net/school-user

  config:
    import: optional:configserver:http://localhost:8888
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: localhost

server:
  port: 8081
